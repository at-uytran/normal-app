<% flash.each do |type, message| %>
  <% if message.present? %>
    <div class="alert <%= alert_class type %> alert-dismissible">
      <button type="button" class="close" data-dismiss="alert" aria-hidden="true">×</button>
      <h5><i class="icon fas fa-ban"></i> Error!</h5>
      <%= message.html_safe %>
    </div>
  <% end %>
<% end %>
